import { RestaurationClient } from "@/components/restauration/RestaurationClient";

/** Page restauration enrichie (événements, prestataires, factures) — conserve le module repas existant */
export default function RestaurationPage() {
  return <RestaurationClient />;
}
